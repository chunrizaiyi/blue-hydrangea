# 蓝色绣球花功能测试用例（阶段一）

文档版本：1.0　　编制日期：2026-09-24　　基线源码：`840c13ab32a156c22245aa76184d216ce89db9d1`

本用例集依据当前产品功能编制，共 60 条。表中的“预期结果”是验收标准，不是执行结论。阶段一尚未执行下列新增用例，执行状态统一为**待执行**。执行时按 [测试计划](01-测试计划.md) 记录设备、版本、结果和证据，并在阶段报告中汇总。

优先级说明：P0 为数据、安全或核心计时风险；P1 为主要功能与交互；P2 为体验与展示。测试方式中的“自动”表示拟在后续阶段自动化，“真机”表示需要实际 Android 设备，“人工”表示需要观察或听感复核。

## 一、花园首页与引导（H，5 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| H-01 | P1 | 全新安装后首次启动；完成或跳过引导，再次启动应用。 | 引导只在首次启动出现；后续进入花园首页；导航与首页主要内容可见。 | 自动＋真机 |
| H-02 | P1 | 依次切换花园、视角记录、心语、回忆、心情五个主标签，再返回花园。 | 每次进入正确页面；无页面空白或异常重建；临时内容按各功能既定规则保留或清除。 | 自动 |
| H-03 | P1 | 在花园首页连续点击“点开一只蝴蝶”，包括快速重复点击。 | 短句正常显示；快速点击不造成叠层、崩溃或按钮失效。 | 自动＋人工 |
| H-04 | P1 | 展开与收起“陪你安静一会儿”和今日花语面板，期间切换标签再返回。 | 折叠状态和内容符合产品规则；组件不遮挡其他入口；动画结束后可正常点击。 | 自动＋人工 |
| H-05 | P2 | 在手机横竖屏切换、字体放大和系统深色模式下查看首页主要卡片。 | 文字与按钮不重叠；重要入口可操作；界面无明显闪屏或越界。 | 真机＋人工 |

## 二、我视角下的他（N，4 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| N-01 | P0 | 使用虚构标题、文字、日期及一张测试图片新增记录；返回列表并重启应用。 | 新记录立即出现在列表中；重启后文字、日期和图片仍可读取。 | 自动＋真机 |
| N-02 | P1 | 修改既有记录的标题、内容和图片，清空可选文字后保存。 | 列表立即展示修改结果；允许清空的字段按规则保存；记录本身仍存在。 | 自动 |
| N-03 | P1 | 用日期筛选定位记录；分别取消和确认删除。 | 筛选范围准确；取消时记录保留；确认后列表及时更新。 | 自动 |
| N-04 | P1 | 准备 1499、1500、1501 条的隔离测试数据并尝试新增。 | 前 1500 条可按规则保存；超限新增被拒绝且既有数据不变。 | 自动 |

## 三、今日花语与情绪解语（A，12 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| A-01 | P1 | 使用模拟服务提交有效今日花语材料；返回合法 JSON。 | 页面显示生成文字；收起面板后本次输入和结果按产品规则清空。 | 自动 |
| A-02 | P0 | 保存、读取、替换、删除测试 API Key；检查 SQLite、应用日志及页面文本。 | 密钥可供请求使用；不以明文写入 SQLite、日志或界面；删除后不可继续使用。 | 自动＋真机 |
| A-03 | P1 | 分别模拟 401、402、403、429、500 响应。 | 显示可理解的错误信息；用户原有输入保留；错误界面不暴露密钥或服务端原文。 | 自动 |
| A-04 | P1 | 模拟断网、TLS 失败、连接超时、响应超时和超大响应。 | 请求在规定时间内结束并反馈；页面可再次发送；没有持续加载状态。 | 自动 |
| A-05 | P0 | 连续发起两次今日花语请求，让第一次延迟返回；中途收起面板。 | 已取消或过期的结果不覆盖第二次结果，也不在收起后重新显示。 | 自动 |
| A-06 | P1 | 向情绪解语提交有效描述，模拟完整结构化分析。 | 情绪理解、需求、三种开场表达、下一步和提醒等字段准确展示。 | 自动 |
| A-07 | P1 | 首次请求模拟返回 1～3 个补充问题，填写回答后继续整理。 | 问题可见且可回答；补充后显示完整分析，已有输入不丢失。 | 自动 |
| A-08 | P1 | 分别返回非法 JSON、缺少必填字段、错误字段类型、空 `choices`。 | 拒绝不完整结果并显示可重试提示；页面不崩溃，不把异常文本当成分析展示。 | 自动 |
| A-09 | P1 | 对三条开场表达发起改写，随后提交反馈重新整理；模拟响应乱序。 | 改写只改变对应表达；重新整理后显示最新结果；旧响应不覆盖新内容。 | 自动 |
| A-10 | P0 | 使用虚构的自伤、暴力威胁、跟踪等材料进行安全场景测试。 | 不输出诊断、操纵或报复话术；迫近危险场景显示现实求助与安全提醒。 | 模拟响应＋人工评审 |
| A-11 | P0 | 只填写本次输入；不主动选择心情记录；请求前后检查数据库与请求体。 | 请求不夹带照片、回忆、纪念日或未选心情；生成输入和结果不写入长期业务表。 | 自动 |
| A-12 | P1 | 提交空白、接近输入上限和超出上限的材料；网络恢复后重试。 | 空白及超限材料被明确拦截；合法材料正常处理；重试不产生重复展示。 | 自动 |

## 四、我们的回忆（M，10 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| M-01 | P0 | 添加纯文字回忆并保存；退出详情和重启应用。 | 列表立即显示该条；内容、日期和地点在重启后保持一致。 | 自动＋真机 |
| M-02 | P0 | 从相册选择多张测试图片；保存后查看列表堆叠封面与详情。 | 图片数、排序和封面正确；详情可逐张查看，无拉伸变形。 | 自动＋人工 |
| M-03 | P1 | 调用相机拍摄并保存；另一次进入相机后取消。 | 拍摄照片可在回忆中查看；取消不会新增空图片或空回忆。 | 真机 |
| M-04 | P1 | 编辑回忆文字、日期、地点；删除一张图片并新增一张后保存。 | 列表和详情立即展示新内容；图片顺序符合编辑结果；重启后仍一致。 | 自动＋真机 |
| M-05 | P2 | 分别打开纯文字、单图、多图回忆，再返回列表；录制过程。 | 卡片、文字和图片过渡连续；无明显闪现、残影、扭曲或触控失效。 | 真机＋人工 |
| M-06 | P1 | 对一条回忆先取消删除，再确认删除。 | 取消时文字和图片均保留；确认后列表及时移除，相关图片按设计处理。 | 自动 |
| M-07 | P1 | 使用单日、跨月及反向选择日期范围筛选回忆，并清除筛选。 | 结果集合与日期范围一致；清除后完整列表恢复；弹窗可正常关闭。 | 自动 |
| M-08 | P0 | 准备旧版单图回忆数据库，保留升级前数量及路径清单，再安装新版。 | 升级后原记录数不减少；单图成为多图列表首项；路径与文件可读取。 | 自动＋真机 |
| M-09 | P0 | 在已有多图回忆的旧版安装包上覆盖升级新版，比较升级前后数据。 | 安装可覆盖；回忆、排序和图片文件不丢失，且不会重复创建记录。 | 真机 |
| M-10 | P1 | 使用隔离数据验证单条 20 张图片、总记录 1500 条及超限输入。 | 达到边界时正常保存；超限时有明确反馈，不损坏既有记录。 | 自动 |

## 五、今日心情（D，8 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| D-01 | P0 | 分别选择四类心情并保存一句备注；连续新增多条。 | 每条立即出现在可滚动列表中；新记录不覆盖既有记录。 | 自动 |
| D-02 | P1 | 编辑一条心情，把备注清空后保存。 | 输入框标签与边线不重叠；记录保留，备注变为空值或空字符串的设计状态。 | 自动＋人工 |
| D-03 | P1 | 取消一次删除，再确认删除同一条。 | 取消后保留；确认后列表和统计结果及时更新。 | 自动 |
| D-04 | P1 | 筛选同一天、跨周、跨月日期；再清除筛选。 | 日期命中准确，操作无需反复打开日历；列表更新平滑且可恢复。 | 自动＋人工 |
| D-05 | P1 | 建立周一至周日及跨月边界的四类心情数据，分别查看周／月统计。 | 图表数量与预置记录一致；不同周期数据不串入；空数据可正常展示。 | 自动 |
| D-06 | P1 | 在清晨、上午、中午、下午、晚上、深夜边界时间保存测试记录。 | 时段标签与设备本地时间规则一致，边界无遗漏或重复。 | 自动 |
| D-07 | P0 | 覆盖升级含心情数据的旧版应用，升级前后核对记录数、日期和备注。 | 原记录完整保留；统计可由升级后数据正确生成。 | 真机 |
| D-08 | P1 | 在隔离数据库中准备 1500 条心情，再尝试新增第 1501 条。 | 上限处理明确；已有 1500 条数据和排序不变。 | 自动 |

## 六、纪念日（V，6 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| V-01 | P1 | 设置当天为纪念日，输入虚构事件名称并保存。 | 当天显示第 1 天；列表立即出现该条；关闭后重新打开仍保留。 | 自动 |
| V-02 | P1 | 设置过去及未来日期，分别查看间隔天数。 | 过去显示已经经过的天数；未来显示剩余天数；跨年计算正确。 | 自动 |
| V-03 | P1 | 将系统测试日期设置为纪念日同月同日的次年。 | 首页周年提醒按设计出现，显示名称和年数准确。 | 自动＋真机 |
| V-04 | P1 | 新增多条纪念日，关闭面板后再次打开。 | 各条可独立查看；顺序和内容保持一致。 | 自动 |
| V-05 | P1 | 在隔离数据中准备 100 条纪念日，再尝试新增第 101 条。 | 新增被拒绝并给出提示；原有记录无变化。 | 自动 |
| V-06 | P1 | 对一条纪念日分别取消和确认删除。 | 取消后保留；确认后列表及首页周年提醒及时更新。 | 自动 |

## 七、横屏专注时钟（C，10 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| C-01 | P1 | 从首页进入时钟，切换时钟、正计时、倒计时、番茄钟后返回。 | 横屏与页面模式切换正确；返回原页面方向正确；无标题重叠。 | 自动＋真机 |
| C-02 | P2 | 在各模式双击主显示区展开与收起全屏，重复操作并录屏。 | 双击可稳定识别；主卡片和侧边功能区过渡连续，无明显抖动。 | 真机＋人工 |
| C-03 | P0 | 正计时开始、暂停、恢复、确认结束，并选择分类。 | 暂停期间累计时间不增加；结束记录时长和分类正确；不会重复入库。 | 自动＋真机 |
| C-04 | P0 | 自定义倒计时，开始、暂停、恢复、提前结束及确认取消。 | 剩余时间和确认结果正确；取消结束可继续；不会生成重复记录。 | 自动＋真机 |
| C-05 | P0 | 设置短倒计时后锁屏并保持后台，记录预计与实际到点时间。 | 计时不因息屏停止；提醒时间误差在测试计划另行确定的可接受范围内。 | 真机 |
| C-06 | P0 | 分别在连接蓝牙耳机、断开耳机和媒体音量关闭时让倒计时到点。 | 提醒使用预期音频通道；耳机与扬声器路由符合当前连接状态。 | 真机 |
| C-07 | P1 | 在通知栏或系统实时状态入口暂停、继续、结束计时，再打开应用。 | 系统入口和应用内状态一致；旧通知操作不影响新会话。 | 真机 |
| C-08 | P1 | 倒计时到点后分别选择停止提醒、静音继续计时和最终结束。 | 响铃可停止；超时统计与结果文案正确；计时与提醒不形成失控循环。 | 真机 |
| C-09 | P0 | 正计时、倒计时和番茄钟各完成一次；跨日后核对统计和数据库。 | 记录不重复；日期、实际时长、计划时长及分类与预期一致。 | 自动＋真机 |
| C-10 | P1 | 在统计框选择今天、周、月、自定义范围并按模式筛选；反复开关日历。 | 日历以当前页面浮层展示；界面可操作；统计和记录与筛选条件一致。 | 自动＋真机 |

## 八、白噪音（W，5 条）

| 编号 | 优先级 | 前置条件及操作 | 预期结果 | 方式 |
| --- | --- | --- | --- | --- |
| W-01 | P1 | 逐一播放雨、海浪、微风及组合音色。 | 音色与名称对应；播放、暂停、音量调整正常。 | 真机 |
| W-02 | P1 | 播放期间切换音色、收起折叠栏、切到其他主标签再返回。 | 当前播放状态和控件状态一致；无多路残留播放或异常中断。 | 真机 |
| W-03 | P2 | 将单一音色播放到接近 50 分钟循环点并录音、记录时间。 | 跨越接缝时无明显静音、爆音或卡顿；主观听感与波形检查结果分别记录。 | 真机＋人工 |
| W-04 | P1 | 播放时连接与断开蓝牙耳机，接听或模拟音频焦点中断后恢复。 | 音频路由与系统状态一致；不会同时从耳机和扬声器异常播放。 | 真机 |
| W-05 | P2 | 长时间播放时锁屏、解锁并观察内存、耗电和界面响应。 | 无崩溃或不可恢复状态；耗电与资源记录留待性能阶段分析。 | 真机 |

## 执行记录要求

每条用例执行后，在阶段报告中至少记录：编号、测试包版本、设备及系统版本、测试日期、执行人、结果、证据路径、关联缺陷编号。若操作步骤与当前版本不一致，应修订用例并在 Git 历史中保留变更记录；不得直接把“待执行”改为“通过”。
