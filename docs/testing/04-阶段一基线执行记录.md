# 阶段一基线执行记录

| 字段 | 实际记录 |
| --- | --- |
| 执行日期 | 2026-09-24（北京时间） |
| 被测源码 | `840c13ab32a156c22245aa76184d216ce89db9d1` |
| 执行环境 | Windows，本地项目英文路径映射 `C:\Users\12713\Documents\blue_hydrangea_work` |
| Flutter / Dart | Flutter 3.44.7 stable；Dart 3.12.2 |
| 已执行范围 | 仓库现有 Dart 测试与 Flutter 静态检查 |
| 未执行范围 | 新设计的 60 条用例、真机功能、系统通知、摄像头、在线 AI、长时间音频 |
| 新增用例分布 | P0：16 条；P1：39 条；P2：5 条，均待执行 |

## 1. 已有自动化测试

执行命令：

```powershell
flutter test --no-pub -r expanded
```

执行结果：**31 项通过，0 项失败，进程退出码 0**；终端末行 `00:00 +31: All tests passed!`。

逐项终端记录见 [2026-09-24 基线终端记录](evidence/2026-09-24-基线终端记录.txt)。可在相同源码提交和 Flutter 环境下使用上述命令复核。

| 测试文件 | 本次通过数 | 当前验证重点 |
| --- | ---: | --- |
| `test/focus_statistics_test.dart` | 11 | 专注记录、日期范围、分类筛选、倒计时达成率 |
| `test/focus_timer_bridge_test.dart` | 2 | 原生计时快照字段与归一化 |
| `test/garden_models_test.dart` | 14 | 回忆和心情模型、纪念日、上限与统计规则 |
| `test/white_noise_service_test.dart` | 4 | 本地长音轨资源和音色组合 |
| **合计** | **31** | **仅为现有 Dart 测试，不代表整机功能覆盖** |

## 2. 静态检查

执行命令：

```powershell
flutter analyze --no-pub
```

执行结果：**0 个问题，进程退出码 0**；终端输出 `No issues found! (ran in 20.5s)`。

## 3. 设备识别情况

执行 `adb devices -l` 得到一台状态为 `device` 的小米设备：型号 `23127PN0CC`，Android 16 / API 36，系统构建号 `OS3.0.302.0.WNCCNXM`。此结果只证明电脑识别到设备；**本阶段没有在手机上执行、安装或修改应用**。

## 4. 结果边界

- 31 项现有测试全部通过，静态检查未报错。执行范围内没有记录产品缺陷。
- 当时的 `test/` 目录主要验证计算规则与模型转换，尚无应用流程、Android 原生或持续集成测试。因此，本报告不评价相册、相机、通知、锁屏、在线 AI 接口和动画表现。
- 当前 Android 构建脚本要求本地固定签名配置，后续接入 GitHub Actions 时须为测试构建设计独立的签名配置，同时保留正式包原签名。
- `pubspec.yaml` 中保留三条 50 分钟 OGG 音轨；本阶段测试只验证资源和组合规则，没有验证真实播放或接缝听感。

## 5. 阶段结论

阶段一完成测试设计和现有测试基线执行。新编制的 60 条用例在本报告日期均未执行。其后续执行情况以对应阶段报告为准，不计入本阶段通过数。
